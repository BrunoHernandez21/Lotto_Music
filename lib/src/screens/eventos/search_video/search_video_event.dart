import 'package:flutter/material.dart';

class SearchVideosEvent extends StatelessWidget {
  static const routeName = 'searchVideosEvent';
  const SearchVideosEvent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SafeArea(
        child: Column(
          children: [
            Container(),
          ],
        ),
      ),
    );
  }
}
