import 'package:flutter/material.dart';

class Adquiridas extends StatelessWidget {
  final TabController controller;
  const Adquiridas({required this.controller, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
